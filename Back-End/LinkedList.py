list = "3x*3+2x-3"

#Puedo tener una lista aparte de las listas de índices que ya creé, a la 3 o a la 2 y  así
#Puedo crear otra lista que me guarde los objetos de las listas creadas.
#Y esa lista de objetos es la que voy a pasar
for i in range(len(list)):
    if list[i] == "x" and list[i+1] == "*" and : #Verifico que sea potencia y que no esté en la lista de objetos creados
#Lo que hago dentro del condicional es agregar el número antes de la x

    print(list[i])